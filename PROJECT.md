# Project Goals & Persona

## Persona

### 운영자
- 1인 개발자, 100개 자동화 수익 파이프라인 축적 목표
- 김프 차익거래 봇은 여러 자동화 시스템 중 하나
- 24/7 무인 운영 전제, 수동 개입 최소화
- 리스크 성향: 보수적, 방향성 중립(시장 중립 차익거래)

### 봇
- 이름: Kimchi Premium Arbitrage Bot
- 역할: Upbit 현물 매수 + Binance 선물 숏으로 김프 차익을 자동 수익화
- 운영 원칙: 감이 아닌 구조적 필터로 진입하고, 측정 가능한 기준으로 청산한다

## 사명 (Mission)

> 가격 방향에 베팅하지 않는다. 한국 거래소와 글로벌 거래소 간의 구조적 가격 차이(김프)를 체계적으로 수익화한다.

최종 목표는 단순 수익률이 아니라,
**설명 가능한 진입, 측정 가능한 엣지, 반복 가능한 수익 구조**를 만드는 것이다.

### 핵심 신념
1. 김프는 한국 시장의 구조적 비효율이며, 이 비효율은 규제·자본 이동 제한으로 인해 지속된다
2. 방향성 중립 포지션(현물 롱 + 선물 숏)은 시장 변동과 무관하게 김프 차이만 추출한다
3. 다중 필터 진입은 단일 지표 진입보다 생존율이 높다
4. 슬리피지와 수수료는 전략의 적이다 — 사전 추정 없이 진입하지 않는다

## 목표

### 구체적 목표
1. 월 3-5% 순수익 (수수료·슬리피지·펀딩비 차감 후)
2. 최대 4개 티커 동시 포지션으로 자본 효율 극대화
3. 7단계 필터 체인으로 저품질 진입 차단
4. 양방향 Trailing Stop으로 진입·청산 타이밍 최적화
5. 모든 거래를 추적 가능하게 기록

### 비목표
- 방향성 매매 (롱/숏 단방향)
- 고빈도 매매 (HFT)
- 단일 거래소 내 차익거래
- 레이턴시 경쟁 (co-location, FPGA)

## 전략 모델

### 김프 차익거래 구조
```text
진입:
  Upbit 현물 매수 (KRW) + Binance 선물 숏 (USDT)
  → 방향성 중립, 순수 김프 차이만 추출

청산:
  Upbit 현물 매도 + Binance 선물 숏 정리
  → 진입 김프 - 청산 김프 = 순이익
```

### 7단계 진입 필터 (스킵 조건 체인)
아래 항목은 "조건을 만족하면 통과"가 아니라, **스킵 조건을 순서대로 검사하는 체인**이다.

```text
1. Balance Check     — 1차 음수 잔고 스킵 + 2차 주문금액 차감 가능 여부 확인
2. Top Ticker        — 거래량 상위 티커만 진입
3. Trailing Stop     — open_min_gimp → open_stop_gimp 저점 추적
4. Spread Skip       — 스프레드 과대(> 0.2%) 시 스킵
5. BTC Anchor Skip   — BTC 대비 괴리 과대 시 스킵
6. RSI Filters       — 양쪽 거래소 4h/15m RSI(35/65) + RSI GAP 부족 시 스킵
7. TS Confirmation   — open_ts_count >= 5 (반등 확인)
```

시뮬레이션 모드의 `open_gimp`/`close_gimp`는 Binance 가격에 환율(`TETHER`)을 곱하지 않는 상대 비교용 비율이다.
라이브 체결 후 `order_open_gimp`에서만 고정 환율을 반영한다.

## 현재 상태 스냅샷

### 확인된 것
- 3-프로세스 아키텍처(main/collect/command) 안정 동작
- WebSocket 실시간 호가 수신 (Upbit + Binance)
- 7단계 진입 필터 체인 구현 완료
- 4-trigger 청산 로직 구현 완료
- 4-layer 슬리피지 관리 시스템 구현 완료
- 라이브/시뮬레이션 듀얼 모드 (코드 토글)
- 텔레그램 명령어 인터페이스

### 아직 미착수인 것
- 2배 레버리지 자본 효율화
- Upbit 랜딩 서비스 통합
- 진입/청산 파라미터 동적 조정
- Docker 컨테이너화

## 인프라

| 구성 | 선택 |
|------|------|
| Language | Python 3.x |
| Async | asyncio, aiohttp |
| WebSocket | websockets |
| 프로세스 관리 | nohup + shell scripts |
| 알림 | Telegram (aiogram + python-telegram-bot) |
| 시각화 | matplotlib |
| 로깅 | TimedRotatingFileHandler (30일 보관) |
| 환경 | conda |

## 로드맵

| Phase | 목표 | 현재 상태 |
|-------|------|----------|
| Phase 0 | 봇 기본 구조 + 시뮬레이션 | 완료 |
| Phase 1 | 라이브 거래 안정화 | 완료 |
| Phase 2 | 2x 레버리지 + 파라미터 최적화 | **진행 예정** |
| Phase 3 | Upbit 랜딩 서비스 통합 | 미시작 |
| Phase 4 | Docker + 운영 자동화 | 미시작 |
